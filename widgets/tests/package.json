{
  "name": "wfca-widget-tests",
  "version": "1.0.0",
  "description": "Security and functional tests for WFCA Fire Widget",
  "scripts": {
    "test": "playwright test",
    "test:security": "playwright test security.spec.ts",
    "test:widget": "playwright test widget.spec.ts",
    "test:api": "playwright test api.spec.ts",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0"
  }
}
